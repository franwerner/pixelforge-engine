const toDecimal = (n:number) => Math.trunc(n * 100) / 100
/**
 * Utilizar esta funciona parsear los valores que se conviertan en flotantes .9999999999 o e+16.
 * Ya que se realizan calculos
 */

export default toDecimal
